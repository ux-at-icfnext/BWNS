permalink: pretty

defaults:
  - scope:
      path: "assets/img/icons"
    values: 
      icon: true
  - scope:
      path: "" # an empty string means all files in the project
      type: "pages"
    values:
      layout: default

#domain: ux-at-icfnextgithub.io       # if you want to force HTTPS, specify the domain without the http at the start, e.g. example.com
#url: https://ux-at-icfnextgithub.io  # the base hostname and protocol for your site, e.g. http://example.com
#baseurl: /BWNS/